{
  "name": "salsicha-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "webpack --mode production",
    "start": "bun src/server.ts",

    "docker:dev": "bun run --watch src/server.ts",
    "docker:build": "webpack --mode production",

    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "bun-types": "latest",
    "sass": "^1.83.4"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "css-loader": "^7.1.2",
    "ejs": "^3.1.10",
    "node-sass": "^9.0.0",
    "normalize.css": "^8.0.1",
    "prisma": "^6.2.1",
    "reflect-metadata": "^0.2.2",
    "sass-loader": "^16.0.4",
    "style-loader": "^4.0.0",
    "ts-loader": "^9.5.2",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1"
  },
  "prisma": {
    "schema": "src/app/prisma/schema.prisma"
  }
}
